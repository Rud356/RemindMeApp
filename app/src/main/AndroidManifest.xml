<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".remind.ReminderApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_clover"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_clover"
        android:supportsRtl="true"
        android:theme="@style/Theme.RemindMeApp"
        tools:targetApi="31"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".fragments.LoadingActivity"
            android:screenOrientation="nosensor"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name="com.example.remindmeapp.fragments.LoginActivity" />
        <activity android:name="com.example.remindmeapp.fragments.RegisterActivity" />
        <activity android:name="com.example.remindmeapp.fragments.MainActivity" />

        <receiver android:name=".remind.ReminderBroadcastReceiver" />
    </application>
</manifest>